# Backend API URL (omit or leave empty to use same-origin relative path '/api')
VITE_API_URL=http://localhost:8000

# Realtime WebSocket server URL (omit or leave empty to use same-origin host)
VITE_SOCKET_URL=http://localhost:8000

# Set to true only for offline development with mock fixtures
VITE_USE_MOCK=false
